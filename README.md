# Chronotype–EEG ML

Machine learning on chronotype (morning/evening) using behavioral + ERP features.

## Setup
python -m venv env
source env/bin/activate     # macOS/Linux
pip install -r requirements.txt